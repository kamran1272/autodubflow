# Legacy specification

The uploaded VideoForge AI specification was not present as a file in this workspace, so there is no source document to move here. The existing VideoForge-oriented UI remains in `apps/dubflow-web` and is preserved while the AutoDubFlow architecture is rebuilt.

Treat that UI and any future copy of the uploaded document as legacy reference material. The canonical product direction is documented in `docs/architecture/overview.md`: autonomous channel automation first, with the media studio retained as a supporting surface.
