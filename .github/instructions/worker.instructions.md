---
applyTo: "apps/worker/**/*.ts"
---

Workers must be idempotent, bounded, observable, and safe to retry. Persist meaningful state transitions and keep provider and media execution behind package interfaces.
