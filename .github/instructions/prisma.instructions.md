---
applyTo: "packages/database/prisma/**/*.prisma"
---

Use Prisma as the source of truth for durable state. Preserve ownership relationships, explicit indexes, and migration-safe schema changes. Do not store secrets in schema defaults or source files.
