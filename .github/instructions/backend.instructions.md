---
applyTo: "apps/api/**/*.ts"
---

Authorize every protected request, enforce resource ownership, validate external input, and enqueue long-running work instead of performing it in the API process.
